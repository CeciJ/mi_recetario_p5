{% extends "base.html.twig" %}

{% block title 'Gérer les ingrédients' %}

{% block body %}
    <div class="container mt-4">

        <div style="margin-bottom:15px;">
            <a href="{{ path('admin.recipe.index') }}" class="btn btn-secondary">Retour à la gestion des recettes</a>
        </div>

        <h1>Gérer les ingrédients</h1>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success">{{ message }}</div>
        {% endfor %}

        <table class="table table-stripped">
            <thead>
                <tr>
                    <th>Nom</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for ingredient in ingredients %}
                <tr>
                    <td>{{ ingredient.name }}</td>
                    <td>
                        <a href="{{ path('admin.ingredient.edit', {id: ingredient.id}) }}" class="btn btn-secondary">Éditer</a>    
                        <form method="post" action="{{ path('admin.ingredient.delete', {id: ingredient.id}) }}" style="display:inline-block;" onsubmit="return confirm('Êtes-vous bien sûr?')">
                            <input type="hidden" name="_method" value="DELETE">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingredient.id) }}">
                            <button class="btn btn-danger">Supprimer</button>
                        </form>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>

        <div class="text-right">
            <a href="{{ path('admin.ingredient.new') }}" class="btn btn-primary">Ajouter un nouvel ingrédient</a>
        </div>
    </div>
    
{% endblock %}