{{ form_start(formOption) }}
    <div class="row">
        <div class="col-md-4">{{ form_row(formOption.name) }}</div><span class="closeForm" style="cursor:pointer;">x</span>
    </div>
    {{ form_widget(formOption) }}
    <button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
{{ form_end(formOption) }}