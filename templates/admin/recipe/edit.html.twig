{% extends "base.html.twig" %}

{% block title 'Éditer la recette' %}

{% block body %} 

    <div style="margin-bottom:15px;">
        <a href="{{ path('admin.recipe.index') }}" class="btn btn-secondary editRecipe backRecipeList">Retour à la liste de recettes</a>
    </div>

    <div class="container mt-4 formEditRecipe">
        <h1>Éditer la recette</h1>
        {{ include("admin/recipe/_form.html.twig", {form: form, button: 'Éditer la recette'}) }}
    </div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('manageRecipe') }}
    <!--<script src="https://cdn.jsdelivr.net/npm/algoliasearch@3/dist/algoliasearch.min.js"></script>
    <script src="https://cdn.jsdelivr.net/autocomplete.js/0/autocomplete.jquery.min.js"></script>-->
{% endblock %}