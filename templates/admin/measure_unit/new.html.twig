{# {% extends 'base.html.twig' %}

{% block title %}New MeasureUnit{% endblock %}

{% block body %}
    <h1>Create new MeasureUnit</h1>

    {{ include('measure_unit/_form.html.twig') }}

    <a href="{{ path('admin.measure_unit.index') }}">back to list</a>
{% endblock %} #}

{{ form_start(formAddMeasureUnit) }}
    <div class="row">
        <div class="col-md-4">{{ form_row(formAddMeasureUnit.unit) }}</div>
    </div>
    {{ form_widget(formAddMeasureUnit) }}
    <button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
{{ form_end(formAddMeasureUnit) }}
