{% extends "base.html.twig" %}

{% block title 'Gérer les unités' %}

{% block body %}
    <div class="container mt-4">

        <div style="margin-bottom:15px;">
            <a href="{{ path('admin.recipe.index') }}" class="btn btn-secondary">Retour à la gestion des recettes</a>
        </div>

        <h1>Gérer les unités</h1>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success">{{ message }}</div>
        {% endfor %}

        <table class="table table-stripped">
            <thead>
                <tr>
                    <th>Nom</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for unit in measure_units %}
                <tr>
                    <td>{{ unit.name }}</td>
                    <td>
                        <a href="{{ path('admin.measure_unit.edit', {id: unit.id}) }}" class="btn btn-secondary">Éditer</a>    
                        <form method="post" action="{{ path('admin.measure_unit.delete', {id: unit.id}) }}" style="display:inline-block;" onsubmit="return confirm('Êtes-vous bien sûr?')">
                            <input type="hidden" name="_method" value="DELETE">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unit.id) }}">
                            <button class="btn btn-danger">Supprimer</button>
                        </form>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>

        <div class="text-right">
            <a href="{{ path('admin.measure_unit.new') }}" class="btn btn-primary">Ajouter une nouvelle unité</a>
        </div>
    </div>
    
{% endblock %}