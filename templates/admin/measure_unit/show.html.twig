{% extends 'base.html.twig' %}

{% block title %}MeasureUnit{% endblock %}

{% block body %}
    <h1>MeasureUnit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ measure_unit.id }}</td>
            </tr>
            <tr>
                <th>Unit</th>
                <td>{{ measure_unit.unit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin.measure_unit.index') }}">back to list</a>

    <a href="{{ path('admin.measure_unit.edit', {'id': measure_unit.id}) }}">edit</a>

    {{ include('measure_unit/_delete_form.html.twig') }}
{% endblock %}
