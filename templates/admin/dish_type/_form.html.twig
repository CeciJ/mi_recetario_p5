{{ form_start(formDishType) }}
    <div class="row">
        <div class="col-md-4">{{ form_row(formDishType.name) }}</div>
    </div>
    {{ form_widget(formDishType) }}
    <button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
{{ form_end(formDishType) }}