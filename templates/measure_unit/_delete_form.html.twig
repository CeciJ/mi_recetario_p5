<form method="post" action="{{ path('admin.measure_unit.delete', {'id': measure_unit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ measure_unit.id) }}">
    <button class="btn">Delete</button>
</form>
