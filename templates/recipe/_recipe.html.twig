<div class="card mb-4">
    <div class="card-body">
        <h5 class="card-title">
            <a href="{{ path('recipe.show', {id: recipe.id, slug: recipe.slug}) }}">{{ recipe.name }}</a>
        </h5>
        <p class="card-text">{{ recipe.type }}, {{ recipe.category }}</p>
    </div>
</div>