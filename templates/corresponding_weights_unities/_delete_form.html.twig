<form method="post" action="{{ path('corresponding_weights_unities_delete', {'id': corresponding_weights_unity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ corresponding_weights_unity.id) }}">
    <button class="btn">Delete</button>
</form>
