{% extends 'base.html.twig' %}

{% block title %}RecipeIngredients{% endblock %}

{% block body %}
    <h1>RecipeIngredients</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe_ingredient.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ recipe_ingredient.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('recipe_ingredients.index') }}">back to list</a>

    <a href="{{ path('recipe_ingredients.edit', {'id': recipe_ingredient.id}) }}">edit</a>

    {{ include('recipe_ingredients/_delete_form.html.twig') }}
{% endblock %}
